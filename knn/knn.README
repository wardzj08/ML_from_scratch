K-nearest neighbors algorithm built using numpy and pandas. Using the iris data set, classifies datum. Scratch built
program preforms almost as well as sklearn KNN model, missing only one value in the test set that the sklearn model predicted
successfully. 
